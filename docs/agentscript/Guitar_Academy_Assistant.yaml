system:
    instructions: "You are an AI Agent."
    messages:
        welcome: |
            Hi, I'm an AI assistant. How can I help you?
        error: "Sorry, it looks like something has gone wrong."

config:
    developer_name: "Guitar_Academy_Assistant"
    default_agent_user: "guitar_academy_assistant@00dgl00000sbuzq653152549.ext"
    agent_label: "Guitar Academy Assistant"
    description: "New agent description"

language:
    default_locale: "en_US"
    additional_locales: ""
    all_additional_locales: False

variables:
    EndUserId: linked string
        source: @MessagingSession.MessagingEndUserId
        description: "This variable may also be referred to as MessagingEndUser Id"
    RoutableId: linked string
        source: @MessagingSession.Id
        description: "This variable may also be referred to as MessagingSession Id"
    ContactId: linked string
        source: @MessagingEndUser.ContactId
        description: "This variable may also be referred to as MessagingEndUser ContactId"
    EndUserLanguage: linked string
        source: @MessagingSession.EndUserLanguage
        description: "This variable may also be referred to as MessagingSession EndUserLanguage"
    VerifiedCustomerId: mutable string
        description: "This variable may also be referred to as VerifiedCustomerId"
    MSID: mutable string
        description: "Working copy of the MessagingSession Id, refreshed on every message"
    currentPage: mutable string
        description: "The page the student is on: home, catalog, or video. Refreshed on every message."
    currentVideoId: mutable string
        description: "The Guitar_Video__c Id of the video the student is currently watching."

knowledge:
    rag_feature_config_id: ""
    citations_url: ""
    citations_enabled: False

start_agent agent_router:
    label: "Agent Router"
    description: "Welcome the user and determine the appropriate subagent based on user input"
    reasoning:
        instructions: ->
            | set @variables.MSID = @variables.RoutableId
            | run @actions.get_session
            |     with inRoutableId = @variables.MSID
            |     set @variables.currentPage = @outputs.CurrentPage
            |     set @variables.currentVideoId = @outputs.CurrentVideoId
            |
            | Select the best tool to call based on conversation history and user's intent.
            | If the user asks to speak with a human agent or be transferred, call {!@actions.go_to_escalation}.
            | If the user wants to filter videos, browse the catalog, purchase a video, or manage a subscription, transition to ecommerce using go_to_ecommerce.

        actions:
            get_session: @actions.get_session
                with inRoutableId = @variables.RoutableId
            go_to_off_topic: @utils.transition to @subagent.off_topic
                description: "Transition when the user's request is off-topic or unrelated to Guitar Academy."
            go_to_ambiguous_question: @utils.transition to @subagent.ambiguous_question
                description: "Transition when the user's request is too vague or unclear to act on."
            go_to_escalation: @utils.transition to @subagent.escalation
                description: "Transition when the user wants to speak with a human agent."
            go_to_ecommerce: @utils.transition to @subagent.ecommerce
                description: "Transition when the user wants to filter videos, purchase a video, subscribe, or manage their subscription."

    actions:
        get_session:
            label: "Get Messaging Session"
            description: "Fetches the MessagingSession record to refresh currentPage and currentVideoId on every message."
            target: "flow://Get_Messaging_Session"
            inputs:
                inRoutableId: string
                    label: "Routable Id"
                    description: "The MessagingSession Id — always pass @variables.RoutableId"
                    is_required: True
            outputs:
                CurrentPage: string
                    label: "Current Page"
                    description: "Value of MessagingSession.Current_Page__c"
                CurrentVideoId: string
                    label: "Current Video Id"
                    description: "Value of MessagingSession.Current_Video_Id__c"

subagent off_topic:
    label: "Off Topic"
    description: "Redirect conversation to relevant topics when user request goes off-topic"
    reasoning:
        instructions: ->
            | Your job is to redirect the conversation to relevant topics politely and succinctly.
              The user request is off-topic. NEVER answer general knowledge questions. Only respond to general greetings and questions about your capabilities.
              Do not acknowledge the user's off-topic question. Redirect the conversation by asking how you can help with questions related to the pre-defined topics.

subagent ambiguous_question:
    label: "Ambiguous Question"
    description: "Redirect conversation to relevant topics when user request is too ambiguous"
    reasoning:
        instructions: ->
            | Politely guide the user to provide more specific details about their request.

subagent escalation:
    label: "Escalation"
    description: "Handles requests from users who want to transfer or escalate their conversation to a live human agent."
    reasoning:
        instructions: ->
            | If a user explicitly asks to transfer to a live agent, call {!@actions.escalate_to_human} to complete the escalation.
        actions:
            escalate_to_human: @utils.escalate
                description: "Call this tool if the user indicates that they wish to escalate to a human agent."

subagent ecommerce:
    label: "Ecommerce"
    description: "Helps students browse the video catalog, filter videos, purchase individual videos, and manage subscriptions."
    reasoning:
        instructions: ->
            | run @actions.debug_context
            |     with currentPage = @variables.currentPage
            |     with currentVideoId = @variables.currentVideoId
            |     with contactId = @variables.ContactId
            |     with routableId = @variables.RoutableId
            |
            | Review the debug output above. If currentPage is "catalog", call filter_catalog.
            | If currentPage is "video", call purchase_video.
            | If currentPage is neither, tell the student which page to navigate to.
         actions:
            debug_context: @actions.debug_context
                with currentPage = @variables.currentPage
                with currentVideoId = @variables.currentVideoId
                with contactId = @variables.ContactId
                with routableId = @variables.RoutableId
            filter_catalog: @actions.filter_catalog
                available when @variables.currentPage == "catalog"
                with level = ...
                with category = ...
                with inRoutableId = @variables.RoutableId
            purchase_video: @actions.purchase_video
                available when @variables.currentPage == "video"
                with videoId = @variables.currentVideoId
                with contactId = @variables.ContactId

    actions:
        debug_context:
            label: "Debug Agent Context"
            description: "Returns the current values of agent variables for debugging. Always call this first."
            target: "apex://GuitarDebugController"
            inputs:
                currentPage: string
                    label: "Current Page"
                    description: "Pass @variables.currentPage"
                    is_required: False
                currentVideoId: string
                    label: "Current Video Id"
                    description: "Pass @variables.currentVideoId"
                    is_required: False
                contactId: string
                    label: "Contact Id"
                    description: "Pass @variables.ContactId"
                    is_required: False
                routableId: string
                    label: "Routable Id"
                    description: "Pass @variables.RoutableId"
                    is_required: False
            outputs:
                message: string
                    label: "Debug Output"
                    description: "Variable values from the agent context"
                    is_displayable: True
                    filter_from_agent: False
        filter_catalog:
            label: "Filter Video Catalog"
            description: "Filters the video catalog by skill level and/or category. Only call when currentPage is catalog."
            target: "apex://GuitarVideoController"
            inputs:
                level: string
                    label: "Level"
                    description: "Skill level: Beginner, Intermediate, or Advanced"
                    is_required: False
                category: string
                    label: "Category"
                    description: "Category e.g. Blues, Rock, Classical"
                    is_required: False
                inRoutableId: string
                    label: "Messaging Session Id"
                    description: "The active MessagingSession Id — always pass @variables.RoutableId"
                    is_required: False
            outputs:
                message: string
                    label: "Result Message"
                    description: "Confirmation of what filter was applied"
                    is_displayable: True
                    filter_from_agent: False
        purchase_video:
            label: "Purchase Video"
            description: "Purchases the video the student is currently watching. Only call when currentPage is video."
            target: "apex://GuitarPurchaseController"
            inputs:
                videoId: string
                    label: "Video Id"
                    description: "The Guitar_Video__c record Id — pass @variables.currentVideoId"
                    is_required: True
                contactId: string
                    label: "Contact Id"
                    description: "The student Contact Id — pass @variables.ContactId"
                    is_required: True
            outputs:
                message: string
                    label: "Result Message"
                    description: "Purchase confirmation or error"
                    is_displayable: True
                    filter_from_agent: False

connection messaging:
    outbound_route_type: "OmniChannelFlow"
    outbound_route_name: "flow://Route_to_Human_Flow"
    escalation_message: "One moment, I'm transferring our conversation to get you more help."
    adaptive_response_allowed: True
