// This won't work from Apex - use Tooling API instead
// Just a placeholder to check deploy state
System.debug('Guitar_Video__c field count: ' + Schema.SObjectType.Guitar_Video__c.fields.getMap().size());
